{
  "name": "learning-backend",
  "version": "1.0.0",
  "description": "This a backend app using node,express and mongodb to learn backend in more depth.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon -r dotenv/config --experimental-json-modules src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "backend",
    "node",
    "javascript"
  ],
  "author": "Vaibhav Bagate",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongoose": "^8.0.3"
  }
}
